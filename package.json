{
  "name": "github-stats-creator",
  "version": "1.0.0",
  "description": "GitHub Action to generate SVG stats cards from GitHub commit history",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --license licenses.txt",
    "all": "npm run build"
  },
  "keywords": ["github", "actions", "svg", "stats", "contributions"],
  "author": "marttp",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.5.0"
  }
}
